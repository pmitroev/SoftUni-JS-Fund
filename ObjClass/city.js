function solve(cityInfo){
    cityInfo = {
        name: 'Stara Zagora',
        area: 110000,
        population: 150000,
        country: 'Bulgaria',
        postcode: 6000,
    }

    for(let i = 0; i < cityInfo.length; i++){
        console.log(`${cityInfo.key} - > `);
    }
}